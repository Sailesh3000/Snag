services:
  backend:
    build: .
    ports:
      - "8765:8765"
    environment:
      - SNAG_HOST=0.0.0.0
      - SNAG_PORT=8765
      - SNAG_LOG_LEVEL=INFO
      - SNAG_CORS_ORIGINS=["*"]
      - SNAG_OLLAMA_URL=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - backend-data:/app/data
    restart: unless-stopped

volumes:
  backend-data:
